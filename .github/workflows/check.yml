name: hygiene-check
on:
  pull_request:
  push:
    branches: [main]
jobs:
  gates:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Prove every gate can fail
        run: |
          chmod +x scripts/*.sh scripts/*.py
          ./scripts/check-hygiene.sh --selftest
      - name: Check hygiene
        run: ./scripts/check-hygiene.sh HEAD
      - name: Verify generated templates are fresh
        run: ./scripts/build-template.py --profile full --check
      - name: Show what an adopter sees
        run: |
          ./scripts/install.sh --list
          ./scripts/phases.py --phase 0
