# Reference entry template

Copy to `docs/references/<name>.md`.

# <name>

## TL;DR

One to two sentences: what does this repo/tool/skill set do for you?

## Provides (2-3 checkable rules or workflow snippet)

- Rule 1: concrete, checkable — quote or paraphrase with source section.
- Rule 2: ...
- Rule 3 (optional): ...

## License

e.g. Apache-2.0 / MIT / etc. Must be compatible with MIT or noted as optional integration (not vendored).

## Use with

When this pairs well with core: e.g. "if project uses conventional commits" or "if you want pre-commit automated slop check alongside llm-tells".

## Install / wiring

```bash
# snippet — how to install upstream repo/skill/tool
# e.g. npm install -D @commitlint/cli, or git clone, or skills/... path
```

How to wire into code-hygiene flow: which phase it slots into (0 discovery, 1 spec, 2 plan, 3 code, 4 review, 5 changelog).

## When not to use

- Condition where this conflicts with upstream's own rules or is overkill.
- When project already enforces different convention that covers same.

## Link

- Upstream: https://github.com/<org>/<repo> or https://...
- Docs: https://...
