# CLAUDE.md — see AGENTS.md template

Canonical template lives in `templates/AGENTS.md`. This file is a thin shim for Claude Code — same pattern as root CLAUDE.md pointing to root AGENTS.md, avoids drift from maintaining two full copies.

For Claude Code:

```bash
cat skills/catalog/SKILL.md  # or templates/AGENTS.md full workflow
```

Workflow is 6-phase cumulative with 4Q gate: (a) wrong (b) materially better way (not just split) (c) missing (d) boundaries explicit + positive framing.
